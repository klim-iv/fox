<html>
	<body>
		<h2><%= cur_dir %></h2>
		<ol>
			<% files.each { |f| %>
			<li>
			<% if f["is_dir"] %>
				<a href="/list<%= f["name"] %>">
					<%= f["name"] %> = <%= session_id %>
				</a>
			<% else %>
				<%= f["name"] %> = <%= session_id %>
			<% end %>
			</li>
			<% } %>
		</ol>
	</body>
</html>
